Le repo contient un notebook principal notebooks/main_notebook.ipynb ainsi que deux fichiers Python à placer dans investment_lab/ : signal.py (construction du signal composite alpha_t) et mapping.py (fonctions de mapping sigmoid, tanh, linéaire et step pour transformer le signal en strike et maturité cibles). Ces deux fichiers s'appuient sur le code du cours (backtest, option_selection, option_trade).
